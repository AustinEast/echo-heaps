# class paths
-cp src

# entry point
-main Main

# libraries
-lib heaps
-lib echo

# flags
-debug
-D windowSize=640x640

# echo macro - adds a field named `entity` of type `Entity` to the `echo.Body` class
--macro echo.Macros.add_data("entity", "Entity")
